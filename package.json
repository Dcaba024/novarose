{
  "name": "novaroseai-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.12.1",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @novarose/web",
    "build": "npm run build --workspace @novarose/web",
    "lint": "npm run lint --workspace @novarose/web",
    "typecheck": "npm run typecheck --workspace @novarose/web"
  }
}
